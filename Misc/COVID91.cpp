class Solution {
public:
    float findSlope(vector<vector<int>> coord){
        
    }
    bool checkStraightLine(vector<vector<int>>& coord) {
        int n = coord.size();
        
        if(n == 2)
            return  true;
        
        float m = findSlope(coord);
        for(int i=2; i<n; i++){
            
        }
    }};