#include<bits/stdc++.h>
using namespace std;

int main() {
    // int t;
    // cin>>t;
    // while(t--) {
        char s[n], int marks[n];
        cin>>n;
        for(int i=0;i<n;i++)
            {
                cin>>s[i]>>marks[i];
            }
            for(int i=0;i<n;i++)
                cout<<marks[i];
        //studentRecord(s, marks);
    //}
    return 0;
}