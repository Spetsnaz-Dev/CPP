//Group Anagrams Together
#include<bits/stdc++.h>
using namespace std;
void groupAnagrams(string str, int n)
{
    map<string, int> mp;
    for(int i=0;i<n;i++)
         m 
}
int main()
{
    int t;
    cin.ignore("\n");
    while (t--)
    {
        int n;
        cin>>n;
        string str[n];
        for(int i=0;i<n;i++)
            cin>>str[i];
        groupAnagrams(str, n);
    }
    return 0;
}